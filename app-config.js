// Single source of truth for the app's display name across these pages.
// Rename the app: change this one value. Note: <title> and <meta name="description">
// are deliberately left as static text (not tokenized) since search engines and
// link-preview crawlers don't execute JS — sync those manually on a rename.
const APP_NAME = "SubioTracker";
